<IfModule mod_rewrite.c>
    RewriteEngine On

    # Stop rewriting if already pointing to public/ (prevents infinite loop)
    RewriteRule ^public/ - [L]

    # Route everything else into the public directory
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
